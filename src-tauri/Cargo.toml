[package]
name = "splitwave"
version = "1.1.0"
description = "Audio routing app for macOS, Linux and Windows. Build a node graph of inputs, effects, and outputs"
license = "MIT"
edition = "2021"

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "splitwave_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

# rubato's sinc resampler is unusable in dev without optimisation — a single
# 1024-frame block takes ~16 ms vs ~500 µs at -O3, so the DSP worker can't
# keep real time and input rings overflow. Optimise just the resampler stack;
# leaving the rest of the workspace (tauri, web stack, our crate) at -O0
# keeps clean builds fast.
[profile.dev.package.rubato]
opt-level = 3
[profile.dev.package.realfft]
opt-level = 3
[profile.dev.package.rustfft]
opt-level = 3

# audiopus_sys uses CMake. At opt-level 0 CMake selects the debug MSVC runtime,
# while Rust uses the release runtime even in dev/test profiles on Windows.
[profile.dev.package.audiopus_sys]
opt-level = 3
[profile.test.package.audiopus_sys]
opt-level = 3

# DeepFilterNet inference (tract) is far heavier than the FFT crates above. At
# -O0 a single block's inference blows the DSP block deadline, the worker
# stalls, and the voice gets glitchy. Optimise the tract stack + deep_filter in
# dev so the NoiseSuppressor keeps real time.
[profile.dev.package.tract-core]
opt-level = 3
[profile.dev.package.tract-linalg]
opt-level = 3
[profile.dev.package.tract-data]
opt-level = 3
[profile.dev.package.tract-nnef]
opt-level = 3
[profile.dev.package.tract-onnx]
opt-level = 3
[profile.dev.package.tract-onnx-opl]
opt-level = 3
[profile.dev.package.tract-hir]
opt-level = 3
[profile.dev.package.tract-pulse]
opt-level = 3
[profile.dev.package.tract-pulse-opl]
opt-level = 3
[profile.dev.package.deep_filter]
opt-level = 3

[dependencies]
tauri = { version = "2", features = ["unstable"] }
tauri-plugin-opener = "2"
tauri-plugin-store = "2"
tauri-plugin-dialog = "2"
tauri-plugin-updater = "2"
tauri-plugin-process = "2"
tauri-plugin-os = "2"
tauri-plugin-autostart = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
cpal = "0.15"
rtrb = "0.3"
rubato = "0.16"
audio_thread_priority = "0.36"
ebur128 = "0.1"
flac-codec = "1.3"
hound = "3.5"
opus = "0.3"
ogg = "0.9"
mp3lame-encoder = "0.2"
symphonia = { version = "0.6", features = ["all"] }
base64 = "0.22"
tokio = { version = "1", features = ["rt-multi-thread", "sync", "time", "net"] }
webrtc = "0.11"
tokio-tungstenite = { version = "0.26", features = ["native-tls"] }
futures-util = "0.3"
flate2 = "1"
bytes = "1"
cuid2 = "0.1"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
ts-rs = { version = "12", features = ["serde-compat"] }
ndarray = "0.15"
# CLAP plugin hosting. clack is git-only and pre-1.0; pin an exact rev because
# it has no semver releases and warns of breaking API changes.
clack-host = { git = "https://github.com/prokopyl/clack.git", rev = "f874e858ad576c601367b3038af5088a4e671744" }
clack-extensions = { git = "https://github.com/prokopyl/clack.git", rev = "f874e858ad576c601367b3038af5088a4e671744", features = ["clack-host", "audio-ports", "gui", "timer", "state", "params", "latency", "raw-window-handle_06"] }
raw-window-handle = "0.6"
vst3 = "0.3"

[target.'cfg(target_os = "linux")'.dependencies]
pipewire = { version = "0.9", features = ["v0_3_44"] }
# Native volume read/set and event-driven change notifications. Talks to
# pipewire-pulse; high-level, so no subprocess or polling like wpctl/pactl.
libpulse-binding = "2"
freedesktop-desktop-entry = "0.8"
freedesktop-icons = "0.4"
resvg = "0.47"
dirs = "5"
# VST3: modules are loaded with dlopen, and X11 editors hand the host file
# descriptors to poll from its run loop.
libc = "0.2"
# Bundled roots for the updater's reqwest client (see `check_for_updates`):
# sandboxed AppImages/Flatpak can't see the host trust store, so the update
# check falls back to webpki-roots. macOS/Windows keep the platform verifier.
webpki-roots = "0.26"
rustls = { version = "0.23", default-features = false, features = ["ring", "std", "tls12"] }

[target.'cfg(target_os = "windows")'.dependencies]
windows-core = "0.61"
png = "0.17"
reqwest = { version = "0.12", default-features = false, features = ["blocking", "rustls-tls-webpki-roots"] }
sha2 = "0.10"
winreg = "0.55"
zip = { version = "2.4", default-features = false, features = ["deflate"] }
windows = { version = "0.61", features = [
    "Win32_Foundation",
    "Win32_Security",
    "Win32_Security_Authorization",
    "Win32_Media_Audio",
    "Win32_Media_Audio_Endpoints",
    "Win32_System_Com",
    "Win32_System_Com_StructuredStorage",
    "Win32_System_Registry",
    "Win32_System_Variant",
    "Win32_System_Threading",
    "Win32_System_ProcessStatus",
    "Win32_Storage_FileSystem",
    "Win32_Devices_DeviceAndDriverInstallation",
    "Win32_Devices_FunctionDiscovery",
    "Win32_Devices_Properties",
    "Win32_UI_Shell",
    "Win32_UI_Shell_PropertiesSystem",
    "Win32_UI_WindowsAndMessaging",
    "Win32_Graphics_Gdi",
    # VST3 modules are plain DLLs loaded by hand.
    "Win32_System_LibraryLoader",
] }

[target.'cfg(target_os = "macos")'.dependencies]
embed_plist = "1.2"
objc2 = "0.6"
# Audio Unit hosting. Generated, maintained bindings to the AudioToolbox C API;
# coreaudio-sys is deliberately not used (it is in maintenance mode and
# coreaudio-rs itself moved to the objc2 crates).
objc2-audio-toolbox = "0.3.2"
objc2-core-audio-types = "0.3.2"
objc2-core-foundation = { version = "0.3.2", features = [
    "CFData", "CFError", "CFPropertyList",
    # VST3 modules load as bundles: bundleEntry takes the CFBundleRef itself.
    "CFBundle", "CFURL", "CFString",
] }
objc2-foundation = { version = "0.3", features = [
    "NSArray", "NSString", "NSEnumerator", "NSError", "NSObject",
    "NSObjCRuntime", "NSDictionary", "NSValue", "NSData", "NSURL",
    # AU editors: the plugin's Cocoa view comes out of a bundle we load by URL.
    "NSBundle", "NSGeometry"
] }
objc2-app-kit = { version = "0.3", features = [
    "NSWorkspace", "NSRunningApplication"
] }

[target.'cfg(not(any(target_os = "android", target_os = "ios")))'.dependencies]
tauri-plugin-global-shortcut = "2.3.2"
# SCK integration uses a hand-rolled Swift static library at `native/`,
# compiled by build.rs (direct `swiftc`, no SwiftPM). The screencapturekit
# crate is rejected here because its build.rs uses `swift build` which fails
# on the host's CLT (PackageDescription dylib ABI mismatch).
# objc2 stays for NSWorkspace-based app listing.

# tract's arm64 NEON kernels are GNU-as only and can't assemble to COFF, so
# DeepFilterNet won't compile on Windows ARM64. Excluded there; the effect
# degrades to passthrough (see noise_suppressor.rs).
[target.'cfg(not(all(target_os = "windows", target_arch = "aarch64")))'.dependencies]
deep_filter = { git = "https://github.com/Rikorose/DeepFilterNet", tag = "v0.5.6", package = "deep_filter", default-features = false, features = ["tract", "default-model"] }
